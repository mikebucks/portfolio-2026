# Copy this file to `.env.local` and fill in the values.

# Contact form. Access key from https://web3forms.com (public, client-side).
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=

# Mixpanel project token (public, send-only). Unset = tracking is a no-op.
NEXT_PUBLIC_MIXPANEL_TOKEN=
